repos:
  - repo: local
    hooks:
      - id: npm-test
        name: Run npm test
        entry: npm test
        language: system
        pass_filenames: false
        stages: [commit]

      - id: npm-build
        name: Run npm build
        entry: npm run build
        language: system
        pass_filenames: false
        stages: [commit]
